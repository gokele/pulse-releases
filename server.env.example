# Pulse 服务端配置
#
# 放在 pulse-server 二进制的同一个目录里，程序启动时会自己读。
# 也可以用 -config 指定别的路径。
#
# 优先级：命令行参数 > 环境变量 > 这个文件 > 内置默认值
#
# 这里只放「必须在进程启动前就知道」的参数。
# 站点名、访客密码、上报节奏、历史保留天数、汇率、节点、Ping 任务、
# 管理员账号密码 —— 这些全部在后台改，不在这个文件里。
#
# 格式就是 KEY=VALUE，一行一个，# 开头是注释。
# 值可以不加引号；成对的单/双引号会被自动剥掉。


# ---------------------------------------------------------------------------
# 监听地址（必填）
# ---------------------------------------------------------------------------
# :8899            监听所有网卡的 8899 端口
# 127.0.0.1:8899   只监听本机；前面用 Nginx / Caddy 反代时推荐这样，
#                  面板不会直接暴露在公网
# [::1]:8899       IPv6 本机
PULSE_LISTEN=:8899


# ---------------------------------------------------------------------------
# 机密字段的加密密钥（可选）
# ---------------------------------------------------------------------------
# 数据库里的节点密钥、访客密码、管理员密码哈希、GitHub Client Secret
# 都用这把密钥加密。
#
# 留空（保持注释状态）时，程序会在 /var/lib/pulse/secret.key 自动生成一把，
# 绝大多数情况这样就行。
#
# 只有在「密钥要统一托管、不想落在磁盘上」时才需要显式配置。
# 必须是 64 位十六进制（32 字节），生成方式：
#   openssl rand -hex 32
#
# ⚠️ 换了这个值，旧数据里的机密字段就解不开了。
#PULSE_SECRET_KEY=


# ---------------------------------------------------------------------------
# GitHub API 地址（可选）
# ---------------------------------------------------------------------------
# 面板用它检查新版本。机器访问不了 api.github.com 时，指向一个镜像即可；
# 留空则用官方地址。注意这只影响「检查版本」，二进制下载走的是
# Release 的 download 地址。
#PULSE_GITHUB_API=https://api.github.com


# ---------------------------------------------------------------------------
# 数据目录（可选，一般不用改）
# ---------------------------------------------------------------------------
# 默认就是二进制同目录下的 data/ —— 数据库、加密密钥都在那儿。
# 整个目录（程序 + 这个配置 + data）拷到别处就能直接跑，不用改任何路径。
#
# 只有想把数据放到别的盘时才需要改。
#PULSE_DATA=/data/pulse
